# Секреты и ключи доступа

## SSH и система
- Пароль SSH для стационара: `1981vbars`
- Пароль для Ubuntu на ноутбуке: `1959vbars`

## Baserow
- Логин: gruzinov.dmitry.sergeevich@gmail.com
- Пароль: `1I9N59!_09&`
- Database token (чтение/запись): `S79owXt1Q4XlvhGW8q825aOntwVI0wiE`
- JWT получается через POST /api/user/token-auth/

# Управление секретами

- **Все секреты (токены, пароли, ключи) хранятся в `~/my-ai-stack/analytics/.env`**.
- В коде используй `python-dotenv` или `os.getenv()` для доступа к ним.
- Никогда не вставляй секреты напрямую в код или в правила.
- Для Docker-контейнеров пароли к базам данных заданы в `docker-compose.yml` (не выгружаются в правила).
EOF

# Общие напоминания для всех проектов

- Всегда проверяй наличие актуальных правил в `~/.roo/rules/` и `.roo/rules/` проекта.
- Секреты хранятся в `~/my-ai-stack/analytics/.env`, не вставляй их в код.
- Для работы с Baserow используй API, а не прямой SQL (если нет специальной задачи).
- При генерации кода обязательно добавляй обработку ошибок и логирование
- Все секреты хранятся в переменных окружения или `.env` файлах. Никогда не коммитить.

