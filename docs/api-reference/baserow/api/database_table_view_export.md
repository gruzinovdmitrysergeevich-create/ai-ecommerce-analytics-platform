# Database table view export

## GET /api/database/view/get-public-view-export/{job_id}/

****  
*operationId: `get_public_view_export_job`*

**Параметры:**

- `job_id` (path) — The signed job id to lookup information about.

---

## POST /api/database/view/{slug}/export-public-view/

****  
*operationId: `export_publicly_shared_view`*

**Параметры:**

- `slug` (path) — Select the view you want to export.

**Тело запроса:**


---
