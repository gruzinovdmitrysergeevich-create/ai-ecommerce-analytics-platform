# Database table export

## POST /api/database/export/table/{table_id}/

****  
*operationId: `export_table`*

**Параметры:**

- `table_id` (path) — The table id to create and start an export job for

**Тело запроса:**


---

## GET /api/database/export/{job_id}/

****  
*operationId: `get_export_job`*

**Параметры:**

- `job_id` (path) — The job id to lookup information about.

---
